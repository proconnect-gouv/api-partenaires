server {
  listen 80;

  location / {
    proxy_pass http://partners:3000;
    # the contract: SET, never append — the client cannot forge its own IP
    proxy_set_header X-Forwarded-For $remote_addr;
  }
}
